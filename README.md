# Lifeline

Lifeline is an activity tracker built to test DDD in a strict form: business logic should live in the domain layer, not be spread across application services.

The application layer exists only to orchestrate domain logic and infrastructure implementations in order to execute business use cases.
